# Render Blueprint — deploys the Next.js app as a Node web service.
#
# In the Render dashboard:  New +  →  Blueprint  →  connect this repo.
# Render reads this file, then prompts for the Gemini secret marked `sync: false`.
services:
  - type: web
    name: vedaai-assignment
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm run start
    # `/` is a static 200; /api/health does live provider probes (too slow for a health check).
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
      # Non-secret config (safe to keep in the blueprint).
      - key: GEMINI_BASE_URL
        value: https://generativelanguage.googleapis.com/v1beta
      # Secrets — set in the Render dashboard, never committed.
      - key: GEMINI_API_KEY
        sync: false
